import 'package:flutter/material.dart';

class StudyInteractionsScreen extends StatelessWidget {
  const StudyInteractionsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
